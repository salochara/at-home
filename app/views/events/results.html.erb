<div class="container-fluid">
  <div class="row">
    <div class="col-md-7">
      <div id="events" class="card-results test-card" >
          <h2>Events:</h2>
          <% @events.each do |event| %>
            <div class="col-xs-12 col-sm-6">
              <%= render event %>
            </div>
          <% end %>
      </div>

    </div>
        <div id="map" data-markers="<%= @markers.to_json %>" data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
        </div>
  </div>
</div>

